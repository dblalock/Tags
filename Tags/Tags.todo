
X add a "new" button at the bottom of the table / bottom of screen
	make it work
	// not a button at bottom of screen

X edit names in some not-horrible way

figure out why the 1st event gets lost when you collapse the same cell after opening it, but not when collapse a different cell

make it actually modify types
	-when you add/delete stuff
	-when you rename stuff

make it persist modifications to userdefaults

make tags table / screen

